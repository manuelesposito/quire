{
  "name": "@quire/patterns",
  "version": "0.0.0",
  "description": "Composed blocks built from components — SettingsPage, ListTablePage, Dashboard, and the metabox-to-card unifier.",
  "license": "GPL-2.0-or-later",
  "type": "module",
  "main": "./src/index.ts",
  "files": ["dist", "src"],
  "dependencies": {
    "@quire/components": "workspace:*",
    "@quire/tokens": "workspace:*"
  },
  "scripts": {
    "build": "echo \"TODO\"",
    "lint": "echo \"no-op\"",
    "test": "echo \"no-op\""
  }
}
