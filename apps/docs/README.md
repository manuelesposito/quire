# docs

Storybook + the live UI-state inventory. The shop window for `@quire/components` and the
proving ground where every token/component change is validated against the three real
surfaces before it lands.

🌱 Empty scaffold — Storybook setup is the next infrastructure task.
